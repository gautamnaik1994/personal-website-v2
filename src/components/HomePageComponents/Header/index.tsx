import MetaBalls from './MetaBallsV2';
import NameComponent from './Name';

function Header() {
  return (
    <header>
      <MetaBalls />
      <NameComponent />
    </header>
  );
}

export default Header;
